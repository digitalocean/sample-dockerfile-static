spec:
  name: sample-dockerfile-static
  static_sites:
    - name: web
      git:
        branch: main
        repo_clone_url: https://github.com/digitalocean/sample-dockerfile-static.git
      dockerfile_path: Dockerfile.static
      output_dir: /app/public
